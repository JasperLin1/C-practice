// ========================================================
// 📄 src/types/student.ts
// ========================================================
// 在大公司的大型專案中，我們不會把型態宣告跟邏輯混在一起。
// 我們會將型態獨立出來，方便整個專案的不同檔案可以隨時 import 使用。

export interface Student {
    id: string;   // 學號
    name: string; // 姓名
    score: number; // 分數
}
